# Chrome Command in Linux

When launching Chrome from Linux with a new GPU (like RT 5060 TI) use the command below:

```bash
google-chrome --enable-unsafe-webgpu --ozone-platform=x11

```

END of DOC
---
